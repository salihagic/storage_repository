name: storage_repository
description: Abstraction for persisting and reading data to platform specific storage. Best used with dependency injection approach.
version: 0.0.5
repository: https://github.com/salihagic/storage_repository

environment:
  sdk: ">=2.12.1 <3.0.0"
  flutter: ">=2.0.2"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
